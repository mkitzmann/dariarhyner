  {% extends 'base.html.twig' %}

 {% block body %}
        <div class="image-single-caption">
        <div class="image-single-text">
            <span class="bold">{{ artwork.title }}</span><br>
             {{ artwork.series }} / {{ artwork.year }}<br>
            {{ artwork.description }}
            </div>
            <div class="image-links">
            <a href="">PREV</a> / <a href="">OVERVIEW</a> / <a href="">NEXT</a>
            </div>
        </div>

    <div class="content">
  
            <div class="image-single-wrapper" stylen="background-image:url('/art/{{ artwork.image }}')">
            <img src="/art/{{ artwork.image }}">
            </div>

   </div>
     
 {% endblock %}